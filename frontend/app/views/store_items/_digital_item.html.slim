div.media
  div.pull-left
    = image_tag 'box.png', class: 'img-thumbnail media-object'
  div.media-body
    h2.media-heading
      strong = store_item.title
      small.text-success
        == " #{@currency.sign}#{store_item.amount(@currency)}"
    p = store_item.description
